[package]
name = "wactab-daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
evdev = { version = "0.13", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
